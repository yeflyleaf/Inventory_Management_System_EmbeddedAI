<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.dao.UserMapper">

    <!-- 用户实体映射 -->
    <resultMap id="BaseResultMap" type="com.example.backend.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="nickname" property="nickname" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="avatar" property="avatar" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="last_login_at" property="lastLoginAt" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 插入用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, role, status, avatar, phone, email, last_login_at, created_at, updated_at)
        VALUES (#{username}, #{password}, #{nickname}, #{role}, #{status}, #{avatar}, #{phone}, #{email}, #{lastLoginAt}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 更新用户信息 -->
    <update id="update">
        UPDATE user
        SET nickname = #{nickname},
            role = #{role},
            status = #{status},
            avatar = #{avatar},
            phone = #{phone},
            email = #{email},
            last_login_at = #{lastLoginAt},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 修改密码 -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{password},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 修改用户状态 -->
    <update id="updateStatus">
        UPDATE user
        SET status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据ID删除用户 -->
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user
    </select>

    <!-- 根据条件查询用户列表 -->
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT * FROM user
        <where>
            <if test="name != null and name != ''">
                AND (username LIKE CONCAT('%', #{name}, '%') OR nickname LIKE CONCAT('%', #{name}, '%'))
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
